import {Meta} from "@storybook/blocks";

<Meta title="VrxButton/About" />

# VireoxButton

VireoxButton is a simple, highly customizable button.

To use it, import the component and use it as follows:
```js
import {VrxButton} from 'vireoxlibrary';

<VrxButton color={'default'}>
    Click me
</VrxButton>
```

You can use VueJS event handlers to handle button events:

```js
import {VrxButton} from 'vireoxlibrary';

<VrxButton color={'default'} @click={() => console.log('clicked')}>
    Click me
</VrxButton>
```

For API's details and configuration please read the [documentation](./?path=/docs/vrxbutton--docs)
