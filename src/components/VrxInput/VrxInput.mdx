import {Meta} from "@storybook/blocks";

<Meta title="VrxInput/About" />

# VireoxInput

VireoxInput è il componente base per l'inserimento di input.

Il componente utilizza il 100% della larghezza disponibile e aggiorna automaticamente al cambio dell'input la variabile associata.
Per utilizzarlo, importarlo come segue:
```js
import {VrxInput} from 'vireoxlibrary';

const value = ref('')
<VrxInput v-model="value"/>
```

Per dettagli sulle API e le configurazioni leggere la [documentazione](./?path=/docs/vrxinput--docs)
